# 错题本

> 本文件只追加记录操作错误、成因与应对方法，不删除或改写既有记录。

## 2026-03-15：PowerShell 调用 GitHub CLI 时拆分了 `--jq` 表达式

- **错误操作**：在 PowerShell 中向 `gh api` 传入包含空格和字符串拼接的 `--jq` 表达式，命令执行时表达式被拆分为多个参数。
- **错误表现**：GitHub CLI 返回 `accepts 1 arg(s), received 2`，远端文件详情查询未执行。
- **成因**：PowerShell 与外部程序之间的参数传递规则没有按预期保留完整的 jq 表达式。
- **应对方法**：改用不含空格的简单查询，例如 `--jq .html_url`；需要复杂处理时分步查询，避免把复杂 jq 表达式直接放入 PowerShell 外部程序参数。
- **影响范围**：仅远端文件链接核验失败一次；此前的 Git 提交和推送均已成功，不影响仓库内容。

### 日期更正

- 上述标题中的日期 `2026-03-15` 为记录时误填；根据运行环境系统日期，实际日期应为 `2026-08-18`。为遵守错题本只追加、不删改的规则，保留原标题并在此追加更正。
